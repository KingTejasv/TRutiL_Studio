const fs = require('fs'); const code = fs.readFileSync('tools/ffmpeg/ffmpeg.js', 'utf8'); const sandbox = { self: {}, document: { baseURI: 'http://localhost' }, location: { href: 'http://localhost' }, Worker: class {} }; sandbox.window = sandbox.self; const vm = require('vm'); vm.createContext(sandbox); vm.runInContext(code, sandbox); console.log(Object.keys(sandbox)); console.log(Object.keys(sandbox.self));
